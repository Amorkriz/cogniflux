# 生产构建配置（pnpm build 时 Vite 自动读取，mode=production）
# 仅允许 VITE_ 前缀的【公开信息】，严禁任何口令/密钥入库。

# Workspace 网关走真实后端（HTTP/WS，经 nginx 反代 twinspark 容器）
VITE_WORKSPACE_API=real
# API 基址留空：同源相对路径 /api/v1/...（与 nginx 同域反代模型一致）
VITE_API_BASE=
