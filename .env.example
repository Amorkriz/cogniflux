# Cogniflux 环境变量清单（复制为 .env 使用，.env 已被 gitignore）
#
# 安全红线（基线 §13）：
# - 只有 `VITE_` 前缀的变量会被 Vite 注入前端产物，视为【公开信息】。
# - 任何密钥/Token（模型 Key、数据库凭证等）严禁使用 VITE_ 前缀，
#   严禁写入本仓库任何文件；阶段 3 起放在服务端/部署平台环境变量中。

# 站点公开 URL（用于 canonical / OG 等元信息）
VITE_SITE_URL=https://cogniflux.example.com
